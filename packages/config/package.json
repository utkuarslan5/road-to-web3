{
  "name": "@road/config",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'config is source-only'",
    "lint": "echo 'no config lint step yet'",
    "test": "echo 'no config tests yet'"
  }
}
